# url-shortener
A simple Node.js app for shortening a URL

## Setup

\```bash
cp .env.example .env
\```

Edit `.env` with your values. Required variables:

| Variable | Description | Example |
|---|---|---|
| `PORT` | Optional, defaults to 3000 | `3000` |